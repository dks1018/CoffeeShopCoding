#include <stdio.h>
#include <stdlib.h>

int main()
{

    int i;
    int Array[] = {42,1337,0};

    for(i = 0;i < 3 ; i++)
    {
    printf("The values in the array are: %d\n", Array[i]);
    }

    return 0;
}
